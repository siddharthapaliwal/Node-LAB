// Define an object representing a car
const car = {
    make: 'Toyota',
    model: 'Corolla',
    year: 2020
};

// Access and log each property of the object
console.log('Car Make:', car.make);
console.log('Car Model:', car.model);
console.log('Car Year:', car.year);
