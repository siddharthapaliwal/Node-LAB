// Define an array with at least five different elements (numbers, strings, etc.)
let array = [10, "Hello", 23, "World", true];

// Use a for loop to iterate through the array and print each element
console.log("Array Elements:");
for (let i = 0; i < array.length; i++) {
    console.log(`${i + 1}. ${array[i]}`);  // Format the output with index
}
//node+Space+Node26.JS