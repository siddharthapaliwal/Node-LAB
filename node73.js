// Defining variables
const firstName = "John";
const lastName = "Doe";
const age = 30;

// Using a template literal to create a greeting string
const greeting = `Hello, my name is ${firstName} ${lastName} and I am ${age} years old.`;

// Logging the greeting to the console
console.log(greeting);
