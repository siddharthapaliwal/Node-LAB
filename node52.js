// Define an array of string names
const names = ['Charlie', 'Alice', 'Bob', 'Eve', 'Dave'];

// Sort the array alphabetically using the sort method
const sortedNames = names.sort();

// Display the sorted array
console.log('Sorted names:', sortedNames);
