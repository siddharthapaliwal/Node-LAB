// Create two arrays of numbers
const array1 = [1, 2, 3];
const array2 = [4, 5, 6];

// Use the spread operator to combine the arrays into a new array
const combinedArray = [...array1, ...array2];

// Log the result
console.log(combinedArray);  // Output: [1, 2, 3, 4, 5, 6]
